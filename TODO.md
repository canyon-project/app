# TODO

- [ ] **根据 CI 流水线，写文档 self-host 指南**
  - 参考 `.github/workflows/ci.yml` 中的环境与步骤
  - 包含：Node.js、pnpm、PostgreSQL（Docker Compose）、Prisma、依赖安装、构建、运行等
  - 说明 `.env` 配置（如 `DATABASE_URL`）

- [ ] **单元测试完善**
  - 补充 `src/` 下核心模块的 Vitest 单元测试
  - 提高覆盖率

- [ ] **合并到 canyon 主仓库**
